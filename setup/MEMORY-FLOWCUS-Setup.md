# TODD-F0WCUS — 10-Question Setup Wizard

*Complete once. The agent deletes this file after setup.*

---

## Welcome to TODD-F0WCUS

This is your unified memory framework: focus sessions + long-term memory cycling + continuous improvement.

Before your first session, the agent will ask 10 questions to personalize it. Takes ~5-10 minutes.

---

## Questions

The agent will walk through these one at a time:

**1. Custom tags** — What additional tags beyond defaults (#open-loop, #in-session, #decision)?

**2. Priority level names** — What should we call the 3 tiers? Default: HIGH / MED / BACKLOG

**3. Frequently used tools** — What tools do you use in sessions? (e.g., here.now, Supabase, GitHub)

**4. Session preference** — Default session type? (standard = full flow; quick = abbreviated)

**5. File placement** — Where to put files you create? Default: projects/[name]/ for project files, memory/daily/ for logs

**6. GitHub backup** — Auto (every sync) or opt-in? Default: auto

**7. 1% Better** — Enable the improvement prompt? (y/n) If yes, how often? Default: every 5 sessions

**8. Witness Protocol** — Enable the moment-acknowledgment hook? (y/n)

**9. Emotion Check** — Enable the emotional awareness hook? (y/n)

**10. Custom templates** — Any additional session templates beyond quick/standard? (optional)

---

## After Setup

The agent:
1. Fills in `references/focus-best-practices.md`
2. Sets up memory directory structure
3. Deletes this setup file
4. Starts your first session

---

**The agent handles this automatically on your first BEGIN. No manual setup needed.**